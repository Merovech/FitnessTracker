<UserControl x:Class="FitnessTracker.Views.DebugView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessTracker.Views"
             mc:Ignorable="d" 
			 DataContext="{Binding Source={StaticResource Locator}, Path=DebugViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<StackPanel Orientation="Horizontal" Grid.Row="1">
			<TextBlock Text="Record count: " />
			<TextBlock Text="{Binding RecordCount, FallbackValue=0}" />
		</StackPanel>
	</Grid>
</UserControl>
